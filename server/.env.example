# MarkMe 环境变量配置
# 复制此文件为 .env 并修改

# 主服务器端口（前端 + API）
PORT=8080

# MCP HTTP Bridge 端口
MCP_BRIDGE_PORT=8081

# API Key（可选，设置后需要认证）
# MARKME_API_KEY=your-secret-key-here

# 上传文件大小限制（字节），默认 50MB
# MAX_FILE_SIZE=52428800
